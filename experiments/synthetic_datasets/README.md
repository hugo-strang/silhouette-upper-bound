# Synthetic Datasets

This directory contains the synthetic datasets used for experiments. These datasets are generated with a fixed seed to ensure research reproducibility.

## Setup & Generation

To populate this directory with the necessary data and subfolder structures, you must run the script `generate_data.py` first.